multinode: false

# Configure accelerator type for Google TPU
accelerator:
  type: google

modelArtifacts:
  uri: "hf://Qwen/Qwen3-32B"
  size: 200Gi
  authSecretName: "llm-d-hf-token"
  name: "Qwen/Qwen3-32B"
  labels:
    llm-d.ai/inference-serving: "true"
    llm-d.ai/guide: "pd-disaggregation"
    llm-d.ai/hardware-variant: "tpu"
    llm-d.ai/hardware-vendor: "google"
    llm-d.ai/model: "Qwen3-32B"

tracing:
  enabled: true
  otlpEndpoint: "localhost:4317"
  serviceNames:
    routingProxy: "routing-proxy"
  sampling:
    sampler: "always_off"
    samplerArg: "0"

routing:
  servicePort: 8000
  proxy:
    image: ghcr.io/llm-d/llm-d-routing-sidecar:v0.5.0
    connector: nixlv2
    secure: false

decode:
  podAnnotations:
    gke-gcsfuse/volumes: "true"
    gke-gcsfuse/cpu-limit: "0"
    gke-gcsfuse/memory-limit: "0"
    gke-gcsfuse/ephemeral-storage-limit: "0"
  parallelism:
    tensor: 4
  create: true
  replicas: 1
  modelCommand: custom
  extraConfig:
    nodeSelector:
      cloud.google.com/gke-tpu-accelerator: "tpu-v6e-slice"
      cloud.google.com/gke-tpu-topology: "2x2"
  monitoring:
    podmonitor:
      enabled: false
      portName: "vllm"
      path: "/metrics"
      interval: "30s"
  containers:
    - name: "vllm"
      image: "us-west2-docker.pkg.dev/xprof-plugin-project/maniket-vllm-repo/vllm-image:tag3"
      imagePullPolicy: Always
      command:
        - "/bin/bash"
        - "-c"
        - |
          # ROLE: kv_consumer (Receives KV cache from prefill)
          KV_CONFIG="{\"kv_connector\":\"TPUConnector\", \"kv_connector_module_path\" : \"tpu_inference.distributed.tpu_connector\", \"kv_role\":\"kv_consumer\", \"kv_ip\" : \"\"}"
          echo "KV_CONFIG="
          python3 -m vllm.entrypoints.openai.api_server \
            --model "/models/hf-models/Qwen3-32B" \
            --served-model-name "Qwen/Qwen3-32B" \
            --port 8200 \
            --tensor-parallel-size 4 \
            --kv-transfer-config "$KV_CONFIG" \
            --disable-uvicorn-access-log \
            --max-num-seqs 256 \
            --block-size 128 \
            --gpu-memory-utilization 0.90 \
            --max-model-len 8192 \
            --profiler-config '{"profiler": "torch", "torch_profiler_dir": "/mnt/traces", "torch_profiler_with_stack": false}'
      env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: VLLM_TORCH_PROFILER_DIR
          value: "/mnt/traces"
        - name: HOST_TRACER_LEVEL
          value: "2"
        - name: TPU_SIDE_CHANNEL_PORT
          value: "9600"
        - name: TPU_KV_TRANSFER_PORT
          value: "9100"
      ports:
        - containerPort: 8200
          name: vllm
          protocol: TCP
        - containerPort: 9100
          name: tpu-kv-transfer
          protocol: TCP
        - containerPort: 9600
          name: tpu-coord
          protocol: TCP
      resources:
        limits:
          memory: 256Gi
          cpu: "16"
          google.com/tpu: 4
        requests:
          memory: 256Gi
          cpu: "16"
          google.com/tpu: 4
      mountModelVolume: true
      volumeMounts:
        - name: metrics-volume
          mountPath: /.config
        - name: shm
          mountPath: /dev/shm
        - name: torch-compile-cache
          mountPath: /.cache
        - name: model-volume
          mountPath: /models
        - name: profiler-traces
          mountPath: /mnt/traces
      startupProbe:
        httpGet:
          path: /v1/models
          port: vllm
        initialDelaySeconds: 15
        periodSeconds: 30
        timeoutSeconds: 5
        failureThreshold: 120
      livenessProbe:
        httpGet:
          path: /health
          port: vllm
        periodSeconds: 10
        timeoutSeconds: 5
        failureThreshold: 3
      readinessProbe:
        httpGet:
          path: /v1/models
          port: vllm
        periodSeconds: 5
        timeoutSeconds: 2
        failureThreshold: 3
  volumes:
    - name: metrics-volume
      emptyDir: {}
    - name: shm
      emptyDir:
        medium: Memory
        sizeLimit: "16Gi"
    - name: torch-compile-cache
      emptyDir: {}
    - name: profiler-traces
      emptyDir: {}
    - name: model-volume
      csi:
        driver: gcsfuse.csi.storage.gke.io
        volumeAttributes:
          bucketName: rlalwani-vllm
          mountOptions: "implicit-dirs,file-cache:enable-parallel-downloads:true,file-cache:parallel-downloads-per-file:100,file-cache:max-parallel-downloads:-1,file-cache:download-chunk-size-mb:10,file-cache:max-size-mb:-1"

prefill:
  podAnnotations:
    gke-gcsfuse/volumes: "true"
    gke-gcsfuse/cpu-limit: "0"
    gke-gcsfuse/memory-limit: "0"
    gke-gcsfuse/ephemeral-storage-limit: "0"
  parallelism:
    tensor: 4
  create: true
  replicas: 1
  modelCommand: custom
  extraConfig:
    nodeSelector:
      cloud.google.com/gke-tpu-accelerator: "tpu-v6e-slice"
      cloud.google.com/gke-tpu-topology: "2x2"
  monitoring:
    podmonitor:
      enabled: false
      portName: "vllm"
      path: "/metrics"
      interval: "30s"
  containers:
    - name: "vllm"
      image: "us-west2-docker.pkg.dev/xprof-plugin-project/maniket-vllm-repo/vllm-image:tag3"
      imagePullPolicy: Always
      command:
        - "/bin/bash"
        - "-c"
        - |
          # ROLE: kv_producer (Sends KV cache to decode)
          KV_CONFIG="{\"kv_connector\":\"TPUConnector\", \"kv_connector_module_path\" : \"tpu_inference.distributed.tpu_connector\", \"kv_role\":\"kv_producer\", \"kv_ip\" : \"\"}"
          echo "KV_CONFIG="
          python3 -m vllm.entrypoints.openai.api_server \
            --model "/models/hf-models/Qwen3-32B" \
            --served-model-name "Qwen/Qwen3-32B" \
            --port 8000 \
            --tensor-parallel-size 4 \
            --kv-transfer-config "$KV_CONFIG" \
            --disable-uvicorn-access-log \
            --enable-chunked-prefill \
            --block-size 128 \
            --gpu-memory-utilization 0.90 \
            --max-model-len 8192 \
            --profiler-config '{"profiler": "torch", "torch_profiler_dir": "/mnt/traces", "torch_profiler_with_stack": false}'
      env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: VLLM_TORCH_PROFILER_DIR
          value: "/mnt/traces"
        - name: HOST_TRACER_LEVEL
          value: "2"
        - name: TPU_SIDE_CHANNEL_PORT
          value: "9600"
        - name: TPU_KV_TRANSFER_PORT
          value: "9100"
      ports:
        - containerPort: 8000
          name: vllm
          protocol: TCP
        - containerPort: 9100
          name: tpu-kv-transfer
          protocol: TCP
        - containerPort: 9600
          name: tpu-coord
          protocol: TCP
      resources:
        limits:
          memory: 256Gi
          cpu: "16"
          google.com/tpu: 4
        requests:
          memory: 256Gi
          cpu: "16"
          google.com/tpu: 4
      mountModelVolume: true
      volumeMounts:
        - name: metrics-volume
          mountPath: /.config
        - name: shm
          mountPath: /dev/shm
        - name: torch-compile-cache
          mountPath: /.cache
        - name: model-volume
          mountPath: /models
        - name: profiler-traces
          mountPath: /mnt/traces
      startupProbe:
        httpGet:
          path: /v1/models
          port: vllm
        initialDelaySeconds: 15
        periodSeconds: 30
        timeoutSeconds: 5
        failureThreshold: 120
      livenessProbe:
        httpGet:
          path: /health
          port: vllm
        periodSeconds: 10
        timeoutSeconds: 5
        failureThreshold: 3
      readinessProbe:
        httpGet:
          path: /v1/models
          port: vllm
        periodSeconds: 5
        timeoutSeconds: 2
        failureThreshold: 3
  volumes:
    - name: metrics-volume
      emptyDir: {}
    - name: shm
      emptyDir:
        medium: Memory
        sizeLimit: "16Gi"
    - name: torch-compile-cache
      emptyDir: {}
    - name: profiler-traces
      emptyDir: {}
    - name: model-volume
      csi:
        driver: gcsfuse.csi.storage.gke.io
        volumeAttributes:
          bucketName: rlalwani-vllm
          mountOptions: "implicit-dirs,file-cache:enable-parallel-downloads:true,file-cache:parallel-downloads-per-file:100,file-cache:max-parallel-downloads:-1,file-cache:download-chunk-size-mb:10,file-cache:max-size-mb:-1"
